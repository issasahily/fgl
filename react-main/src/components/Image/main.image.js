import React from "react";
import Completion from "./completion.image";
 
const Chatgpt = (props) => {
  return (
    <React.Fragment>
      
      <Completion />
    </React.Fragment>
  );
};

export default Chatgpt;
